[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "alternances-veille"
version = "2.0.0"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]